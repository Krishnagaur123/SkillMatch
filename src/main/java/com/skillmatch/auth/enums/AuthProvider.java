package com.skillmatch.auth.enums;

/**
 * Supported OAuth2 providers. Stored as a VARCHAR(50) in the database.
 * Add new values here when additional providers are integrated.
 */
public enum AuthProvider {
    GOOGLE
}
